# Ignore everything by default
/*

# Allow .gitignore itself
!.gitignore

# Allow rtl directory (needed for git to descend into it)
!rtl/

# Inside rtl/, ignore everything except .v files
rtl/*
!rtl/*.v
